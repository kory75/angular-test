import { FilterByCustomerPipe } from './filter-by-customer.pipe';

describe('FilterByCustomerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCustomerPipe();
    expect(pipe).toBeTruthy();
  });
});
